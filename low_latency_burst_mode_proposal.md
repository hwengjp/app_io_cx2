# 低レイテンシ連射モード 仕様案（H->L トリガ限定）

## 目的
- 親機の低レイテンシ動作で、`H->L` 割り込みをトリガに「1秒間だけ固定値連射」する。
- 連射終了後は、専用の低頻度周期で現在I/O値を送信する待機モードに戻す。
- 連射中は入力変化を無視して値を固定し、チャタリングや揺れの影響を抑える。

## 対象範囲
- 対象: 親機 + 低レイテンシ有効時（`IS_LOW_LATENCY_OPT_PARENT()` が真）
- 非対象: 子機側ロジック、Flash設定構造体の拡張

## 要求仕様の整理
1. 割り込み検出対象は `DI1` と `DI2` の2本のみとし、`H->L` 変化のみをトリガにする（`L->H` は割り込みトリガにしない）。
2. 割り込み発生で「連射中モード」に遷移し、`CONFIG_DEFAULT_FPS` 回送信（= 約1秒）。
   - 周期は `CONFIG_DEFAULT_FPS` に従う。
   - 割り込みが発生した側の信号は `L`、もう一方（DI1/DI2の片割れ）は強制的に `H` としてラッチする。
   - 連射中はこのラッチ値を固定で送信し続ける。
   - 連射中に追加割り込みが来ても送信値は更新しない。
3. 規定回数送信後に「非連射モード」へ遷移。
   - 非連射モードは専用定数周期で送信する。
   - この周期送信では毎回現在のI/Oを読み直して送信する。
   - 連射中に変化した入力も、非連射周期送信で反映される。
   - 次の `H->L` 割り込みで再度連射中モードへ遷移する。
4. 新規変数はRAMのみ（Flash保存なし）。

---

## 現行コードとの対応ポイント

### A. 割り込み検出経路（既存）
- `cbToCoNet_vHwEvent()` の `E_AHI_DEVICE_SYSCTRL` でDI割り込みを処理。
- すでに「押し下げ（Lo検知）」前提のコメントと処理があるため、ここを連射トリガの起点にしやすい。

### B. 送信実行経路（既存）
- 実送信は `i16TransmitIoData()`。
- 現在は `E_STATE_APP_RUNNING_CHILD` のTick処理や低レイテンシ即送信経路から呼ばれる。
- 新仕様では「連射中モード/非連射モード」判定を送信要求の直前に追加するのが自然。

### C. 現値読み取り（既存）
- `E_AHI_DEVICE_TICK_TIMER` 内の `bBTM_GetState()` で `u32BtmBitmap / u32BtmChanged` を更新。
- 新仕様では、連射中に `bmChanged` を無視するか、送信ロジック側でラッチ値固定を優先する。

---

## 実装方針（推奨）

## 1) モード状態を追加
- 追加RAM変数（例）:
  - `bool_t bBurstActive;` 連射中フラグ
  - `uint8 u8BurstRemain;` 残送信回数（初期値=`sAppStg.u8Fps`）
  - `uint16 u16BurstLatchedBm;` 連射中に固定送信するDI値
  - `uint16 u16BurstLatchedUsed;` 連射中に固定送信する有効マスク
  - `uint32 u32IdlePeriodicTick;` 非連射周期送信用タイマ

## 2) H->L 割り込みで連射開始（DI1/DI2限定）
- `E_AHI_DEVICE_SYSCTRL` で `DI1/DI2` の `H->L` を検出したら:
  - `bBurstActive = TRUE`
  - `u8BurstRemain = sAppStg.u8Fps`（1秒相当）
  - ラッチ値を以下で作成して `u16BurstLatchedBm / Used` に保存
    - `DI1` トリガ時: `DI1=L(1), DI2=H(0)`
    - `DI2` トリガ時: `DI2=L(1), DI1=H(0)`
- 連射中に新規割り込みが来ても、ラッチ値更新はしない。

## 3) 連射送信（FPS周期）
- 既存のFPSタイミング判定（`u32CtTimer0 & u8FpsBitMask`）を利用。
- `bBurstActive == TRUE` かつFPSタイミングで送信:
  - `i16TransmitIoData()` に「ラッチ値送信」経路を追加するか、
  - 送信直前に使用ビット/状態ビットを一時上書きして送信。
- 1回送信ごとに `u8BurstRemain--`。
- `u8BurstRemain == 0` で `bBurstActive = FALSE`（非連射モードへ）。

## 4) 非連射モード送信（新規定数周期）
- `config_private.h` に新規定数（例）:
  - `#define CONFIG_IDLE_TX_INTERVAL_MS 200`
- 非連射時はこの周期で現在I/Oを読んで通常送信。
- 連射中に発生した入力変化は、非連射送信タイミングで自然に反映される。

---

## 変更候補の定数（config_private.h）
- `CONFIG_IDLE_TX_INTERVAL_MS`  
  - 意味: 非連射モードの定期送信周期[ms]
  - 推奨初期値: 100〜500ms（用途に応じて調整）
- `CONFIG_BURST_IRQ_DI1_DI2_ONLY`  
  - 意味: 割り込みトリガ対象を DI1/DI2 のみに固定するためのフラグ（または同等の定義）
  - 既定: 有効（本仕様では固定）

---

## 「どのI/Oを有効化するか」の既存変数

既存で関連する変数はあります（用途が異なるため、要件に対しては新設推奨）。

- `gc_u32_PORT_INPUT_MASK`  
  - DIとして扱う全入力ポートのマスク（グローバル）
- `sAppData.sBTM_Config.bmPortMask`  
  - BTM監視対象ポートのマスク
- `sAppStg.u32HoldMask`  
  - ホールド/特殊動作対象のマスク（今回の「割り込み検出対象」とは意味が別）

今回仕様では割り込みトリガ対象を DI1/DI2 のみに固定する。

- 推奨新規:
  - `u32BurstIrqEnableMask`（DI1/DI2のみの固定マスクを保持）
  - 割り込み判定時に `u32ItemBitmap & u32BurstIrqEnableMask` を使う

---

## 競合・注意点
- 既存の低レイテンシ即送信（`TX_OPT_BY_INT`）と二重送信しないよう制御が必要。
- 既存の `u32BtmChanged` 送信経路と非連射周期送信の優先順を明確化する必要がある。
- `IS_APPCONF_OPT_ON_PRESS_TRANSMIT()` 併用時は、現行の特殊処理と競合しやすい。
  - 初期実装は「本仕様有効時はON_PRESS系分岐を無効化」するのが安全。

---

## 実装ステップ案
1. `config_private.h` に非連射周期定数を追加。
2. RAM状態変数を `sAppDataExt` か同等の拡張領域へ追加。
3. `SYSCTRL` 割り込み処理に「連射開始トリガ」「IRQマスク判定」を追加。
4. Tick送信判定に「連射中(FPS固定値) / 非連射(専用周期で現値)」の分岐を追加。
5. 既存低レイテンシ送信との重複防止を追加。
6. 実機確認（1ch→複数ch、連射中の追加変化、連射終了後の追従）を実施。
7. クリーンアップ（6のデバッグ完了後）
   - 低レイテンシ常時ON・連射常時ONを前提に、不要な分岐/旧ルートを削除して整理する。
   - この段階でのみ実施し、6完了前は互換経路を残す。

---

## テスト観点（抜粋）
- `DI1/DI2` の `H->L` のみで連射開始し、`L->H` 単体では開始しないこと。
- DI1トリガ時は `DI1=L, DI2=H`、DI2トリガ時は `DI2=L, DI1=H` が連射中に固定されること。
- 連射中に入力を揺らしても送信値が固定されること。
- 連射回数が `FPS` 回で止まり、非連射周期に戻ること。
- 非連射周期で現在値に追従すること。
- 2台子機で同じ受信挙動になること（同期性確認）。

---

## 追加修正案（DI入力1本化 + 反転補完）

### 背景
- DI1/DI2 を同時に監視すると、同時遷移時に競合して逆値へトグルする場合がある。
- 競合回避のため、入力源を1本化し、もう1本は論理反転で生成する。

### 仕様
1. 入力ソースは `DI1` または `DI2` のどちらか1本を設定で選択する。
2. 非選択側DIは物理入力として読まない（割り込みも受けない）。
3. 送信値は常に以下を満たす。
   - 選択入力を `src` とする
   - `DI_A = src`
   - `DI_B = !src`（もう一方は常に反転）
4. 連射中も非連射中もこのルールを共通で適用する。

### 追加定数案（config_private.h）
- `CONFIG_BURST_INPUT_SOURCE`（値: `1`=DI1, `2`=DI2）
- `CONFIG_BURST_DI_COMPLEMENT_ENABLE`（必要なら。既定 `1`）

### コード修正ポイント
1. **初期化**
   - `u32BurstIrqPortMask` を入力ソース1本のみで構成
   - 例: DI2選択時は DI2ポートビットのみセット

2. **割り込み処理 (`E_AHI_DEVICE_SYSCTRL`)**
   - マスク後の `u32ItemMasked` が非ゼロなら連射開始
   - 連射開始時ラッチ値は「トリガポート」ではなく「選択入力ソース」の現在値から生成

3. **値生成関数の統一**
   - `u16BuildBurstBmFromTrigger()` を置き換え
   - 新関数（例）`u16BuildBmFromSelectedInput(bool_t bSrcLo)` を用意
   - 返却:
     - DI1をsrcにする場合: bit0=`src`, bit1=`!src`
     - DI2をsrcにする場合: bit1=`src`, bit0=`!src`
   - 連射中も非連射中も同じ関数で `u16bm` を作る

4. **有効マスク (`u16bm_used`)**
   - DI1/DI2 は常に有効（初期同期のため）
   - `u16bm_used` に bit0/bit1 を常時OR

5. **非連射周期送信**
   - 現値読み取り時も選択入力1本のみ読んで `u16bm` を生成
   - 非選択側の物理状態は参照しない

### 期待効果
- DI同時遷移レースの解消
- 連射/非連射の値生成ロジック統一による安定化
- デバッグ対象が1入力に限定され、再現・検証が容易

